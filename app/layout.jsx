import './globals.css'

export const metadata = { title: 'Aiagent', description: 'AI workflow app' }

export default function RootLayout({ children }) {
  return <html lang="id"><body>{children}</body></html>
}
