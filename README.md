# Aiagent

Next.js workflow app for AI prompt generation, image generation, uploads, user auth, and dashboard.

## Run

npm install
npm run dev

## Setup

Copy `.env.example` to `.env.local`, connect Supabase, then run the SQL file in `supabase/schema.sql`.